name: Teams News Sync

on:
  # 平日 22:21 JST（UTC 13:21）に1日1回、Teamsチャンネルの新着を「お知らせ」へ取込む。
  # 旧 renew-subscriptions（毎15分）を置き換え、GitHub Actions 消費を大幅削減する。
  schedule:
    - cron: "21 13 * * 1-5"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Call Teams news sync endpoint
        run: |
          HTTP_STATUS=$(curl -s -o /tmp/body -w "%{http_code}" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "https://portal.esco-duct.com/api/admin/notifications/teams-sync")
          echo "HTTP status: $HTTP_STATUS"
          cat /tmp/body || true
          echo ""
          if [ "$HTTP_STATUS" != "200" ]; then
            echo "Teams news sync returned non-200: $HTTP_STATUS"
            exit 1
          fi
          echo "Teams news sync completed."

      - name: Notify Teams on failure
        if: failure()
        uses: daigo-cloud/devlop-cron/.github/actions/notify-failure@main
        with:
          webhook-url: ${{ secrets.MONITORING_TEAMS_WEBHOOK_URL }}
