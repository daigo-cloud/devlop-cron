# モデル運用ルール（指示役と実装役の分離・必須遵守）

```
  指示役（オーケストレーション）                実装役（コードを書く）
  ┌────────────────────────────┐   Agent ツールで委譲   ┌──────────────────┐
  │ 1. Fable 5.1（使用可なら） │ ────────────────────▶ │ Sonnet 5           │
  │ 2. 不可なら Opus 5         │   （中〜大規模の実装）  │ .claude/agents/    │
  │ 3. Opus 5不調→ Opus 4.8    │ ◀──────────────────── │ sonnet-implementer │
  └────────────────────────────┘   実装結果を検収        └──────────────────┘
          │
          └─ 軽量タスク（下記の定義）は指示役が直接実行（委譲しない）
```

- **指示役は Fable 5.1（使用できる場合）**。タスク分解・設計判断・レビュー・検収を担う。
  - Fable 5.1 が使用できない場合は **Opus 5**、Opus 5 の挙動がおかしい場合は **Opus 4.8** を利用する。
  - （2026-07-25 オーナー指示で Opus 系フォールバックを Opus 5 に更新。旧 Opus 4.8→4.7 の段は Opus 5→4.8 に置換。）
  - （2026-07-16 オーナー指示で Fable 5 を指示役に採用。旧: トークン消費過大のため除外・2026-07-06）
  - （2026-09-02 世代更新で Fable 5 → **Fable 5.1**。モデルIDは `claude-fable-5-1`）
- **中〜大規模のコード実装（新機能・複数ファイルにまたがる変更・リファクタリング・テスト追加/修正）は Sonnet 5 で行う。**
  指示役は Agent ツールで `.claude/agents/sonnet-implementer.md`（`model: sonnet` 固定）に
  実装を委譲し、返ってきた変更を検収してからコミットする。
- **軽量タスクは指示役（Fable 5.1）が直接実行する。委譲しない。**（2026-09-16 社長指示・週間トークン上限対策）
  - 軽量タスクの定義: 調査・回答のみ／ドキュメント（`*.md`・`docs/**`・`public/help/**`）の編集／
    設定ファイル・環境変数名・定数の1〜数行の修正／明らかなタイポ・文言・lint 指摘の修正／
    既存テストの実行と結果報告。目安は「変更が1〜2ファイル・数十行以内で、設計判断を伴わない」。
  - 迷ったら Sonnet 5 に委譲する（軽量と言い切れないものは軽量ではない）。
  - 理由: 軽量タスクでもサブエージェント起動のたびに AGENTS.md 全文の再読込などの固定コストが
    かかり、委譲する方が総トークン消費が大きくなるため。
  - 旧ルール（2026-07-06〜2026-09-15）: 「例外はない。1行の修正でも実装は Sonnet 5 に委譲すること」。
    本改定で撤回。
- **この節は全リポジトリ共通ルール**としてオーナー指示により daigo-cloud 配下の各リポジトリの
  AGENTS.md / CLAUDE.md に複製配布済み（2026-07-06・SSOT 例外）。文言を変更する場合は全リポジトリに反映する。
- **新しいリポジトリを作成するときは、この節と `.claude/agents/sonnet-implementer.md` を
  必ず初期コミットに含めること**（AGENTS.md が無ければ新規作成し、CLAUDE.md から `@AGENTS.md` で参照する）。
