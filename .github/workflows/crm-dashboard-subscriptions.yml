# EscoSales（devlop）保存ダッシュボードの定期メール配信（SFの「ダッシュボードの購読」相当・devlop PR #938）。
# 本体側エンドポイント: GET /api/crm/cron/dashboard-subscriptions（CRON_SECRET Bearer・fail-closed）。
# 実行対象の抽出（JST時刻/頻度/二重送信ガード）は本体側 route が行うため、ここは毎時叩くだけ。
name: CRM Dashboard Subscriptions

on:
  schedule:
    # 毎時（分を17にずらしてGitHub Actionsの毎時00分混雑による遅延を緩和。
    # 配信時刻の判定はエンドポイント側がJSTの「時」で行うため分のずれは無害）。
    - cron: "17 * * * *"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger CRM dashboard subscription delivery
        run: |
          set -u
          URL="https://portal.esco-duct.com/api/crm/cron/dashboard-subscriptions"
          echo "== GET ${URL}"
          code=$(curl -sS -o /tmp/out.json -w "%{http_code}" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" "${URL}")
          echo "HTTP ${code}"
          head -c 1200 /tmp/out.json 2>/dev/null || true
          echo ""
          [ "${code}" = "200" ] || { echo "!! crm/dashboard-subscriptions cron failed (HTTP ${code})"; exit 1; }
          echo "CRM dashboard subscription delivery completed."
